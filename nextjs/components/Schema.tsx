interface SchemaProps {
  schemas: Record<string, unknown>[];
}

export function Schema({ schemas }: SchemaProps) {
  return (
    <>
      {schemas.map((schema, i) => (
        <script
          key={i}
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }}
        />
      ))}
    </>
  );
}
